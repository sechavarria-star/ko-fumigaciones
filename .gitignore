# Documentos fuente (viven en Drive, no en git): facturas, extractos, planilla de clientes
Clientes/
Facturas/
Resumenes de Cuenta/
data/extractos_raw/

# Python
__pycache__/
*.pyc

# OS
.DS_Store

# Token de Google cacheado por migracion/apps-script/probar.sh (es una
# credencial: nunca va a git, y de todas formas vence en ~1h)
.token
.clasprc.json

# docs/ es el sitio PUBLICO de GitHub Pages: un PDF de un cliente copiado ahí
# para probar algo queda publicado en internet. Que no pueda pasar por
# accidente.
docs/*.pdf
